---
// src/pages/signin.astro
export const prerender = false;
import favicon from "../../public/favicon.png";
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Registro exitoso</title>
    <link rel="icon" type="image/png" href={favicon.src} />

    <style>
      :root {
        --bg: #0b1220;
        --card: #ffffff;
        --text: #0f172a;
        --muted: #475569;
      }

      body {
        margin: 0;
        min-height: 100vh;
        display: grid;
        place-items: center;
        background: radial-gradient(
            1200px 800px at 50% 10%,
            rgba(37, 99, 235, 0.18),
            transparent 55%
          ),
          var(--bg);
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
        padding: 18px;
      }

      .card {
        width: min(520px, 100%);
        background: var(--card);
        border-radius: 16px;
        padding: 22px;
        box-shadow: 0 18px 60px rgba(2, 6, 23, 0.35);
        border: 1px solid rgba(15, 23, 42, 0.08);
        text-align: center;
      }

      .title {
        margin: 0 0 10px;
        font-size: 1.25rem;
        color: var(--text);
      }

      .text {
        margin: 0;
        color: var(--muted);
        line-height: 1.45;
        font-size: 1rem;
      }
    </style>
  </head>

  <body>
    <main class="card">
      <h1 class="title">✅ Registro completado</h1>
      <p class="text">
        Te has registrado correctamente. Ya puedes usar tus credenciales para ingresar a la plataforma.
      </p>
    </main>
  </body>
</html>
