---
import { supabase } from '../lib/supabaseClient'
const { error } = await supabase.auth.signOut();

// Redirige a login con una URL absoluta
return new Response(null, {
  status: 302,
  headers: {
    Location: '/login'
  }
});
---
