---
import { supabase } from '../lib/supabaseClient'

// Cierra sesión
await supabase.auth.signOut()

// Redirige de forma segura usando URL absoluta
const redirectTo = new URL('/login', Astro.request.url)
return Response.redirect(redirectTo, 302)
---
