---
export const prerender = false;
---

<main class="registro-page">
  <section class="registro-card">
    <h1>Registro de alumno</h1>
    <p class="subtitle">Crea tu cuenta para acceder a las prácticas.</p>

    <form id="registro-form" class="registro-form">
      <div class="form-row">
        <label>
          Nombre(s) *
          <input type="text" name="nombre" required />
        </label>
      </div>

      <div class="form-row">
        <label>
          Apellido paterno *
          <input type="text" name="apellidoPaterno" required />
        </label>
      </div>

      <div class="form-row">
        <label>
          Apellido materno
          <input type="text" name="apellidoMaterno" />
        </label>
      </div>

      <div class="form-row">
        <label>
          Boleta *
          <input type="text" name="boleta" required />
        </label>
      </div>

      <div class="form-row">
        <label>
          Grupo (opcional)
          <input type="text" name="grupo" placeholder="3IV12, 6TM1, etc." />
        </label>
      </div>

      <div class="form-row two-cols">
        <label>
          Contraseña *
          <input type="password" name="password" required minlength="6" />
        </label>

        <label>
          Confirmar contraseña *
          <input type="password" name="passwordConfirm" required minlength="6" />
        </label>
      </div>

      <p id="registro-error" class="msg msg-error" hidden></p>
      <p id="registro-success" class="msg msg-success" hidden></p>

      <button type="submit" id="registro-submit" class="btn-primary">
        Crear cuenta
      </button>
    </form>
  </section>
</main>

<script type="module">
  import { createClient } from '@supabase/supabase-js';

  const supabaseUrl = import.meta.env.PUBLIC_SUPABASE_URL;
  const supabaseAnonKey = import.meta.env.PUBLIC_SUPABASE_ANON_KEY;

  const supabase = createClient(supabaseUrl, supabaseAnonKey);

  const form = document.getElementById('registro-form');
  const errorEl = document.getElementById('registro-error');
  const successEl = document.getElementById('registro-success');
  const submitBtn = document.getElementById('registro-submit');

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    errorEl.hidden = true;
    successEl.hidden = true;
    errorEl.textContent = '';
    successEl.textContent = '';

    const formData = new FormData(form);
    const nombre = formData.get('nombre')?.toString().trim();
    const apellidoPaterno = formData.get('apellidoPaterno')?.toString().trim();
    const apellidoMaterno =
      formData.get('apellidoMaterno')?.toString().trim() || null;
    const boleta = formData.get('boleta')?.toString().trim();
    const grupoNombre = formData.get('grupo')?.toString().trim() || '';
    const email = formData.get('email')?.toString().trim();
    const password = formData.get('password')?.toString();
    const passwordConfirm = formData.get('passwordConfirm')?.toString();

    if (!nombre || !apellidoPaterno || !boleta || !email || !password) {
      showError('Todos los campos marcados con * son obligatorios.');
      return;
    }

    if (password.length < 6) {
      showError('La contraseña debe tener al menos 6 caracteres.');
      return;
    }

    if (password !== passwordConfirm) {
      showError('Las contraseñas no coinciden.');
      return;
    }

    setLoading(true);
    try {
      // 1) Crear usuario en Auth
      const { data, error: signUpError } = await supabase.auth.signUp({
        email,
        password,
      });

      if (signUpError) {
        throw signUpError;
      }

      const user = data.user;
      if (!user) {
        throw new Error('No se pudo obtener el usuario creado.');
      }

      // 2) Resolver Grupo_id (si el usuario escribió un grupo)
      let grupoId = null;

      if (grupoNombre) {
        // Buscar si ya existe un grupo con ese nombre
        const { data: grupoExistente, error: grupoSelectError } = await supabase
          .from('Grupos')
          .select('id')
          .eq('Nombre', grupoNombre)
          .maybeSingle();

        if (grupoSelectError) throw grupoSelectError;

        if (grupoExistente) {
          grupoId = grupoExistente.id;
        } else {
          // Si no existe, lo creamos
          const { data: grupoNuevo, error: grupoInsertError } = await supabase
            .from('Grupos')
            .insert({ Nombre: grupoNombre, Profesor_id: null })
            .select('id')
            .single();

          if (grupoInsertError) throw grupoInsertError;
          grupoId = grupoNuevo.id;
        }
      }

      // 3) Insertar en tabla Usuarios
      const { error: insertError } = await supabase.from('Usuarios').insert({
        Nombre: nombre,
        Apellido_Paterno: apellidoPaterno,
        Apellido_Materno: apellidoMaterno,
        Boleta: boleta,
        Grupo_id: grupoId,  // puede ser null si no puso grupo
        Rol_id: 1,          // 1 = Alumno
        auth_uid: user.id,  // FK a auth.users.id
      });

      if (insertError) {
        throw insertError;
      }

      showSuccess(
        '✅ Cuenta creada correctamente. Revisa tu correo si tienes verificación de email activada.'
      );
      form.reset();
    } catch (err) {
      console.error(err);
      showError(err.message ?? 'Ocurrió un error al crear la cuenta.');
    } finally {
      setLoading(false);
    }
  });

  function showError(msg) {
    errorEl.textContent = msg;
    errorEl.hidden = false;
    successEl.hidden = true;
  }

  function showSuccess(msg) {
    successEl.textContent = msg;
    successEl.hidden = false;
    errorEl.hidden = true;
  }

  function setLoading(isLoading) {
    submitBtn.disabled = isLoading;
    submitBtn.textContent = isLoading ? 'Creando cuenta...' : 'Crear cuenta';
  }
</script>

<style>
  :root {
    --bg-page: #f3f4f6;
    --card-bg: #ffffff;
    --primary: #2563eb;
    --primary-hover: #1d4ed8;
    --text-main: #111827;
    --text-muted: #6b7280;
    --border: #e5e7eb;
    --error: #dc2626;
    --success: #16a34a;
  }

  body {
    margin: 0;
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
      sans-serif;
    background: var(--bg-page);
    color: var(--text-main);
  }

  .registro-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 24px;
  }

  .registro-card {
    width: 100%;
    max-width: 520px;
    background: var(--card-bg);
    border-radius: 16px;
    box-shadow: 0 18px 45px rgba(15, 23, 42, 0.15);
    padding: 24px 28px 28px;
  }

  .registro-card h1 {
    margin: 0 0 4px;
    font-size: 1.8rem;
    font-weight: 700;
    text-align: center;
  }

  .subtitle {
    margin: 0 0 18px;
    text-align: center;
    color: var(--text-muted);
    font-size: 0.95rem;
  }

  .registro-form {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .form-row {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .form-row.two-cols {
    flex-direction: row;
    column-gap: 24px; /* 👈 más espacio entre los dos inputs */
  }

  .form-row.two-cols label {
    flex: 1;
  }

  label {
    font-size: 0.9rem;
    color: var(--text-main);
  }

  input[type="text"],
  input[type="email"],
  input[type="password"] {
    width: 100%;
    padding: 9px 10px;
    border-radius: 8px;
    border: 1px solid var(--border);
    font-size: 0.95rem;
    outline: none;
    transition: border-color 0.15s, box-shadow 0.15s;
    box-sizing: border-box;
  }

  input[type="text"]:focus,
  input[type="email"]:focus,
  input[type="password"]:focus {
    border-color: var(--primary);
    box-shadow: 0 0 0 1px rgba(37, 99, 235, 0.2);
  }

  .btn-primary {
    margin-top: 12px;
    width: 100%;
    padding: 10px 14px;
    border-radius: 999px;
    border: none;
    background: var(--primary);
    color: #fff;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.15s, transform 0.05s;
  }

  .btn-primary:hover:not(:disabled) {
    background: var(--primary-hover);
  }

  .btn-primary:active:not(:disabled) {
    transform: scale(0.98);
  }

  .btn-primary:disabled {
    opacity: 0.7;
    cursor: default;
  }

  .msg {
    margin: 4px 0 0;
    font-size: 0.85rem;
  }

  .msg-error {
    color: var(--error);
  }

  .msg-success {
    color: var(--success);
  }

  @media (max-width: 600px) {
    .registro-card {
      padding: 20px 18px 22px;
    }

    .form-row.two-cols {
      flex-direction: column;
      row-gap: 10px;
    }
  }
</style>
