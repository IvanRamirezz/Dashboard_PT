---
const { href, icon, label, active = false } = Astro.props;
---
<a href={href} class={`flex items-center gap-3 px-3 py-2 rounded-lg ${active ? 'bg-blue-100 text-blue-600 font-semibold' : 'hover:bg-gray-200 text-gray-700'}`}>
  <img src={icon} alt="" class="w-5 h-5" />
  <span>{label}</span>
</a>
